import { SectionStub } from "@/components/section-stub";

export default function EventsPage() {
  return (
    <SectionStub
      title="Events"
      stage="Stage 4"
      description="Upcoming community events with capacity and sign-ups."
    />
  );
}
