import { SectionStub } from "@/components/section-stub";

export default function MaintenancePage() {
  return (
    <SectionStub
      title="Maintenance"
      stage="Stage 5"
      description="Submit requests and track open and closed items."
    />
  );
}
