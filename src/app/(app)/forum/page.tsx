import { SectionStub } from "@/components/section-stub";

export default function ForumPage() {
  return (
    <SectionStub
      title="Forum"
      stage="Stage 3"
      description="Post messages, reply, react, and tag by category."
    />
  );
}
