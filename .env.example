# WCV — environment variables
# Copy to .env and fill in. NEVER commit .env (it is gitignored).
# Only NEXT_PUBLIC_* variables are exposed to the browser.

# ---- Supabase ----
# WCV runs on the SHARED "upvoteme" Supabase project. To avoid clashing with
# other apps in that project, all WCV tables live in the `wcv` Postgres schema
# and all WCV storage buckets are prefixed `wcv-`.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
# Prefer the modern publishable key (sb_publishable_...); the legacy anon JWT
# also works.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SERVER ONLY — used in Server Actions / Edge Functions. Never expose to client.
SUPABASE_SERVICE_ROLE_KEY=
# Postgres schema that WCV owns (do not change).
WCV_DB_SCHEMA=wcv
# Project-global storage buckets are namespaced for WCV.
SUPABASE_STORAGE_BUCKET=wcv-forum-media

# ---- App ----
# Base URL used for auth redirects and outgoing email links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Gate for /bootstrap (promotes the current user to building admin once).
SUPERADMIN_SECRET=

# ---- Email (magic links + notifications via Resend) ----
# On the SHARED Supabase project we do NOT use Supabase SMTP (it is project-wide
# and owned by another app). WCV generates the magic link server-side and sends
# it via Resend from its own address. The domain must be verified in Resend.
RESEND_API_KEY=
EMAIL_FROM=West Complex Village <noreply-live-together@pisan.me>

# ---- Web Push (added in a later stage) ----
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# ---- Optional: observability ----
SENTRY_DSN=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# NOTE: Google OAuth client ID/secret are configured in the Supabase dashboard
# (Auth > Providers > Google), NOT here. Set the redirect URL to
# ${NEXT_PUBLIC_SITE_URL}/auth/callback for local, preview, and production.
#
# SHARED PROJECT: do NOT change the project Site URL or email templates — they
# are project-wide and owned by another app. Instead just ADD this app's URLs
# to Auth > URL Configuration > Redirect URLs (additive):
#   ${NEXT_PUBLIC_SITE_URL}/**  and  http://localhost:3000/**
# Google + magic link both return via /auth/callback (code exchange) using the
# per-request redirectTo/emailRedirectTo, so no shared setting needs editing.
# The `wcv` schema must be added to Project Settings > API > Exposed schemas.
